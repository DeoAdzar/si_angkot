import 'package:flutter/widgets.dart';

class TrackingStatusScreen extends StatelessWidget {
  const TrackingStatusScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("Tracking Status");
  }
}
